# This is a proto-engine
# It must be merged with my mates's code


def use_input(character, input):
	print(input, 'is being used to modify', character, "'s properties")

def use_chat(character, msg):
	print(character, "wrote", msg, "in the chat")
